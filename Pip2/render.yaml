services:
  - type: worker
    name: pip-bot
    runtime: node
    plan: starter  # $7/month for 24/7 uptime
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DISCORD_TOKEN
        sync: false  # Set this in Render dashboard
      - key: DISCORD_CLIENT_ID
        sync: false  # Set this in Render dashboard
      - key: DISCORD_GUILD_ID
        sync: false  # Set this in Render dashboard
      - key: NODE_ENV
        value: production
