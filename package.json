{
  "name": "rollcloud",
  "version": "1.1.2",
  "description": "Dice Cloud Integration for Roll20",
  "scripts": {
    "build": "node build.js",
    "build:chrome": "node build.js && echo 'Chrome build: dist/chrome/'",
    "build:firefox": "node build.js && echo 'Firefox build: dist/firefox/'",
    "build:experimental": "node build.js --experimental",
    "build:exp": "node build.js --experimental",
    "clean": "node -e \"const fs=require('fs'); if(fs.existsSync('dist'))fs.rmSync('dist',{recursive:true}); if(fs.existsSync('dist-experimental'))fs.rmSync('dist-experimental',{recursive:true});\""
  },
  "keywords": [
    "dicecloud",
    "roll20",
    "browser-extension",
    "chrome-extension",
    "firefox-addon"
  ],
  "author": "",
  "license": "MIT"
}
